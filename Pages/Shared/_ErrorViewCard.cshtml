@model string

<div class="uk-card uk-card-default uk-card-hover uk-card-body">
    <h3 class="uk-card-title">Error</h3>
    @if (Model != null)
    {
        <p class="uk-text-left">@Model</p>
    }
</div>
